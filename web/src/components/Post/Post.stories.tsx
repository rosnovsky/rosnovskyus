import React from 'react';
import { action } from '@storybook/addon-actions';
import { PostMetadata } from './PostMetadata';

export const Tags = () => <PostMetadata />;

export default {
  title: 'Blog Post',
  component: Tags,
};
